# 빌드 산출물
dist/
.astro/
node_modules/

# 파이썬
__pycache__/
*.py[cod]
.pytest_cache/
.venv/

# 환경
.env
.env.*
!.env.example

# OS
Thumbs.db
desktop.ini
nul

# ★ 자동 채집한 초안 — 저장소에 넣지 않는다.
# 이 저장소는 공개다. 초안은 작업 노트에서 기계가 뽑아낸 것이라 내부 경로·
# 미공개 정보가 섞여 있을 수 있다. draft:true 는 "사이트에 안 뜬다"를 보장할 뿐
# "공개 저장소에 안 들어간다"를 보장하지 않는다 — 그건 여기서 막는다.
# 사람이 확인해 src/content/cases/ 로 옮기는 순간부터 추적된다.
drafts/

# 사실 감사 결과 — 내부 절대경로가 들어 있어 공개 저장소에 넣지 않는다
FACT-AUDIT.md
